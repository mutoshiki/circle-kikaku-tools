# Project Agent Rules

- Use caveman ultra for project communication unless user says otherwise.
- CSS cleanup order: header/app-shell, settlement cards/tags, theme/09, breakpoint policy.
- Work one CSS owner at a time. Detect duplicate selectors, move rules to owner, run static tests, check browser console.
