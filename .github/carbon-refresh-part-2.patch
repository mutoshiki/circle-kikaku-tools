diff -ruN '--exclude=assets/vendor/carbon/carbon-entry.min.js' '--exclude=.git' carbon-refresh-base/assets/css/settlement/car-inputs/03-extra-costs.css carbon-refresh-worktree/assets/css/settlement/car-inputs/03-extra-costs.css
--- carbon-refresh-base/assets/css/settlement/car-inputs/03-extra-costs.css	2026-08-20 02:39:08.000000000 +0000
+++ carbon-refresh-worktree/assets/css/settlement/car-inputs/03-extra-costs.css	2026-08-20 02:50:39.699685165 +0000
@@ -109,21 +109,31 @@
   min-height: 48px;
 }
 
-@media (max-width: 360px) {
+@media (max-width: 640px) {
   #settlementCarEditModal .seisan-extra-row {
-    grid-template-columns: minmax(44px, 1fr) 68px 104px 48px;
-    gap: 3px;
+    grid-template-columns: minmax(0, 1fr) minmax(112px, 0.75fr) 48px;
+    gap: var(--space-2);
   }
 
+  #settlementCarEditModal .seisan-extra-field:first-child { grid-column: 1 / -1; }
   #settlementCarEditModal .seisan-extra-field--amount,
-  #settlementCarEditModal .seisan-extra-field--amount :is(cds-text-input, cds-number-input) {
-    width: 68px;
-    max-width: 68px;
-  }
-
+  #settlementCarEditModal .seisan-extra-field--amount :is(cds-text-input, cds-number-input),
   #settlementCarEditModal .seisan-extra-field--type,
   #settlementCarEditModal .seisan-extra-field--type cds-select {
-    width: 104px;
-    max-width: 104px;
+    width: 100%;
+    max-width: none;
+  }
+
+  #settlementCarEditModal .seisan-extra-row--labeled > .seisan-icon-btn {
+    margin-top: calc(var(--font-size-caption) * 1.2 + var(--space-1));
+  }
+}
+
+@media (max-width: 360px) {
+  #settlementCarEditModal .seisan-extra-row {
+    grid-template-columns: minmax(0, 1fr) 48px;
   }
+  #settlementCarEditModal .seisan-extra-field--type { grid-column: 1 / 2; }
+  #settlementCarEditModal .seisan-extra-field--amount { grid-column: 1 / 2; }
+  #settlementCarEditModal .seisan-extra-row .seisan-icon-btn { grid-column: 2; grid-row: 2; }
 }
diff -ruN '--exclude=assets/vendor/carbon/carbon-entry.min.js' '--exclude=.git' carbon-refresh-base/assets/css/settlement/car-inputs/04-edit-modal.css carbon-refresh-worktree/assets/css/settlement/car-inputs/04-edit-modal.css
--- carbon-refresh-base/assets/css/settlement/car-inputs/04-edit-modal.css	2026-08-20 02:39:08.000000000 +0000
+++ carbon-refresh-worktree/assets/css/settlement/car-inputs/04-edit-modal.css	2026-08-20 03:01:57.907950579 +0000
@@ -39,3 +39,20 @@
     scroll-padding-bottom: calc(var(--space-6) + env(safe-area-inset-bottom));
   }
 }
+
+@media (max-width: 640px) {
+  /* Vehicle-cost editing is transactional and form-heavy; on Carbon's small
+   * breakpoint it uses the full available viewport rather than a floating
+   * desktop-dialog gutter. Header/body/footer remain the native Carbon modal. */
+  #settlementCarEditModal::part(dialog) {
+    inset: 0;
+    top: 0;
+    left: 0;
+    width: 100vw;
+    max-width: 100vw;
+    height: 100dvh;
+    max-height: 100dvh;
+    margin: 0;
+    transform: none;
+  }
+}
diff -ruN '--exclude=assets/vendor/carbon/carbon-entry.min.js' '--exclude=.git' carbon-refresh-base/assets/css/settlement/car-inputs/05-mobile-inputs.css carbon-refresh-worktree/assets/css/settlement/car-inputs/05-mobile-inputs.css
--- carbon-refresh-base/assets/css/settlement/car-inputs/05-mobile-inputs.css	2026-08-20 02:39:08.000000000 +0000
+++ carbon-refresh-worktree/assets/css/settlement/car-inputs/05-mobile-inputs.css	2026-08-20 02:50:39.699771306 +0000
@@ -9,13 +9,14 @@
 
 @media (max-width: 420px) {
   #settlementCarEditModal .seisan-gas-section-head {
-    grid-template-columns: minmax(0, 1fr) auto;
-    gap: 8px;
-    margin-top: 8px;
-    padding-top: 10px;
+    grid-template-columns: minmax(0, 1fr);
+    gap: var(--space-3);
+    margin-top: 0;
+    padding-top: 0;
   }
 
-  #settlementCarEditModal .seisan-times-toggle {
-    max-width: 196px;
+  #settlementCarEditModal .seisan-rental-type-group {
+    width: 100%;
+    max-width: none;
   }
 }
diff -ruN '--exclude=assets/vendor/carbon/carbon-entry.min.js' '--exclude=.git' carbon-refresh-base/assets/css/settlement/car-inputs/06-times-rental.css carbon-refresh-worktree/assets/css/settlement/car-inputs/06-times-rental.css
--- carbon-refresh-base/assets/css/settlement/car-inputs/06-times-rental.css	2026-08-20 02:39:08.000000000 +0000
+++ carbon-refresh-worktree/assets/css/settlement/car-inputs/06-times-rental.css	2026-08-20 02:50:39.699847416 +0000
@@ -1,14 +1,10 @@
-/* Times rental fee support */
-#settlementCarEditModal .seisan-times-toggle-field {
-  display: flex;
+/* Times rental fee support. The vehicle type is a mutually-exclusive Carbon
+ * radio group rather than an on/off toggle. */
+#settlementCarEditModal .seisan-rental-type-group {
+  display: block;
   align-self: start;
   justify-self: end;
-  justify-content: flex-end;
   min-width: 0;
-}
-#settlementCarEditModal .seisan-times-toggle {
-  display: block;
-  width: auto;
   max-width: 100%;
 }
 #settlementCarEditModal .seisan-car-row.is-times-rental .seisan-fuel-field {
diff -ruN '--exclude=assets/vendor/carbon/carbon-entry.min.js' '--exclude=.git' carbon-refresh-base/assets/css/settlement/car-inputs/07-extra-candidates.css carbon-refresh-worktree/assets/css/settlement/car-inputs/07-extra-candidates.css
--- carbon-refresh-base/assets/css/settlement/car-inputs/07-extra-candidates.css	2026-08-20 02:39:08.000000000 +0000
+++ carbon-refresh-worktree/assets/css/settlement/car-inputs/07-extra-candidates.css	2026-08-20 02:50:39.699955155 +0000
@@ -17,7 +17,7 @@
 
 #settlementCarEditModal .seisan-extra-candidate-list {
   display: grid;
-  grid-template-columns: repeat(2, minmax(0, 1fr));
+  grid-template-columns: minmax(0, 1fr);
   gap: var(--space-2);
   min-width: 0;
 }
@@ -56,12 +56,8 @@
   opacity: 0.76;
 }
 
-@media (min-width: 600px) {
+@media (min-width: 672px) {
   #settlementCarEditModal .seisan-extra-candidate-list {
-    grid-template-columns: repeat(3, minmax(0, 1fr));
+    grid-template-columns: repeat(2, minmax(0, 1fr));
   }
 }
-
-@media (max-width: 340px) {
-  #settlementCarEditModal .seisan-extra-candidate-list { grid-template-columns: minmax(0, 1fr); }
-}
diff -ruN '--exclude=assets/vendor/carbon/carbon-entry.min.js' '--exclude=.git' carbon-refresh-base/assets/css/settlement/page-shell/05-error-states.css carbon-refresh-worktree/assets/css/settlement/page-shell/05-error-states.css
--- carbon-refresh-base/assets/css/settlement/page-shell/05-error-states.css	2026-08-20 02:39:08.000000000 +0000
+++ carbon-refresh-worktree/assets/css/settlement/page-shell/05-error-states.css	2026-08-20 03:03:28.155520136 +0000
@@ -48,9 +48,7 @@
   border-color: color-mix(in srgb, var(--ui-danger, var(--color-danger)) 54%, var(--ui-panel-border));
   background: color-mix(in srgb, var(--semantic-danger-soft) 68%, var(--ui-panel-bg));
 }
-.seisan-car-summary-row {
-  box-shadow: var(--ui-readable-shadow);
-}
+.seisan-car-summary-row { box-shadow: none; }
 .seisan-cost-type-badge {
   border-width: 0;
 }
diff -ruN '--exclude=assets/vendor/carbon/carbon-entry.min.js' '--exclude=.git' carbon-refresh-base/assets/js/carbon-entry.js carbon-refresh-worktree/assets/js/carbon-entry.js
--- carbon-refresh-base/assets/js/carbon-entry.js	2026-08-20 02:39:08.000000000 +0000
+++ carbon-refresh-worktree/assets/js/carbon-entry.js	2026-08-20 02:56:55.898290816 +0000
@@ -5,6 +5,7 @@
 import '@carbon/web-components/es/components/data-table/index.js';
 import '@carbon/web-components/es/components/notification/toast-notification.js';
 import '@carbon/web-components/es/components/tag/index.js';
+import '@carbon/web-components/es/components/tabs/index.js';
 import '@carbon/web-components/es/components/text-input/index.js';
 import '@carbon/web-components/es/components/select/index.js';
 import '@carbon/web-components/es/components/checkbox/index.js';
diff -ruN '--exclude=assets/vendor/carbon/carbon-entry.min.js' '--exclude=.git' carbon-refresh-base/assets/js/core/modal-controller.js carbon-refresh-worktree/assets/js/core/modal-controller.js
--- carbon-refresh-base/assets/js/core/modal-controller.js	2026-08-20 02:39:08.000000000 +0000
+++ carbon-refresh-worktree/assets/js/core/modal-controller.js	2026-08-20 02:47:47.630784066 +0000
@@ -265,7 +265,7 @@
             carEditModal.addEventListener('sanpo:modal-hiding', event => {
                 const validateAndSave = global.validateAndSaveSettlementCarEditBeforeClose;
                 if (typeof validateAndSave === 'function') {
-                    if (!validateAndSave()) event.preventDefault();
+                    if (!validateAndSave(event.detail?.reason || 'dismiss')) event.preventDefault();
                     return;
                 }
                 global.saveSettlementCarEditDraft?.();
diff -ruN '--exclude=assets/vendor/carbon/carbon-entry.min.js' '--exclude=.git' carbon-refresh-base/assets/js/features/events/02-static-header-events.js carbon-refresh-worktree/assets/js/features/events/02-static-header-events.js
--- carbon-refresh-base/assets/js/features/events/02-static-header-events.js	2026-08-20 02:39:08.000000000 +0000
+++ carbon-refresh-worktree/assets/js/features/events/02-static-header-events.js	2026-08-20 02:56:55.899012811 +0000
@@ -58,10 +58,11 @@
             overflow.setAttribute('aria-label', 'アプリメニュー');
             overflow.setAttribute('align', 'bottom-end');
             overflow.querySelector('[slot="icon"]')?.remove();
-            if (!overflow.querySelector('.app-switcher-glyph')) {
+            if (!overflow.querySelector('[data-carbon-icon="switcher"]')) {
                 const glyph = document.createElement('span');
-                glyph.className = 'app-switcher-glyph';
+                glyph.className = 'app-switcher-icon';
                 glyph.slot = 'icon';
+                glyph.dataset.carbonIcon = 'switcher';
                 glyph.setAttribute('aria-hidden', 'true');
                 overflow.prepend(glyph);
             }
@@ -106,13 +107,24 @@
                 ['tab-list', view === 'list' && allocationType === 'car'],
                 ['tab-team', view === 'list' && allocationType === 'team']
             ];
+            let selectedValue = 'car';
             states.forEach(([id, active]) => {
                 const tab = byId(id);
                 if (!tab) return;
                 tab.classList.toggle('active', active);
-                if (active) tab.setAttribute('aria-current', 'page');
-                else tab.removeAttribute('aria-current');
+                tab.toggleAttribute('selected', active);
+                if (active) {
+                    tab.setAttribute('aria-current', 'page');
+                    selectedValue = tab.getAttribute('value') || selectedValue;
+                } else {
+                    tab.removeAttribute('aria-current');
+                }
             });
+            const tabBar = byId('view-toggle-bar');
+            if (tabBar) {
+                tabBar.setAttribute('value', selectedValue);
+                if (customElements.get('cds-tabs')) tabBar.value = selectedValue;
+            }
 
             const carTab = byId('tab-list');
             const teamTab = byId('tab-team');
@@ -148,10 +160,12 @@
         teamTab.id = 'tab-team';
         teamTab.dataset.view = 'list';
         teamTab.dataset.allocationType = 'team';
+        teamTab.setAttribute('value', 'team');
         teamTab.classList.remove('active');
         teamTab.removeAttribute('aria-current');
         teamTab.setAttribute('aria-label', '班割');
         carTab.dataset.allocationType = 'car';
+        carTab.setAttribute('value', 'car');
         carTab.setAttribute('aria-label', '車割');
 
         setDestinationLabel(sheetTab, '共有画面');
diff -ruN '--exclude=assets/vendor/carbon/carbon-entry.min.js' '--exclude=.git' carbon-refresh-base/assets/js/features/events/04-settlement-input-events.js carbon-refresh-worktree/assets/js/features/events/04-settlement-input-events.js
--- carbon-refresh-base/assets/js/features/events/04-settlement-input-events.js	2026-08-20 02:39:08.000000000 +0000
+++ carbon-refresh-worktree/assets/js/features/events/04-settlement-input-events.js	2026-08-20 02:47:47.630032942 +0000
@@ -182,6 +182,13 @@
             }
         });
 
+        document.addEventListener('cds-radio-button-group-changed', event => {
+            const target = event.target;
+            if (!target?.matches?.('.seisan-car-row [data-field="rentalType"]')) return;
+            target.value = event.detail?.value ?? target.value;
+            commitRentalTypeChange(target);
+        });
+
         document.addEventListener('cds-toggle-changed', event => {
             const target = event.target;
             if (commitRentalTypeChange(target)) return;
diff -ruN '--exclude=assets/vendor/carbon/carbon-entry.min.js' '--exclude=.git' carbon-refresh-base/assets/js/features/lock-protection.js carbon-refresh-worktree/assets/js/features/lock-protection.js
--- carbon-refresh-base/assets/js/features/lock-protection.js	2026-08-20 02:39:08.000000000 +0000
+++ carbon-refresh-worktree/assets/js/features/lock-protection.js	2026-08-20 02:56:55.899210113 +0000
@@ -49,7 +49,7 @@
 function updateBottomNavigationLockIndicators() {
     const scopes = normalizeEditLockScopes();
     const configs = [
-        { scope: 'allocation', tabId: 'tab-list', baseLabel: '車割/班割' },
+        { scope: 'allocation', tabId: 'tab-list', baseLabel: '車割' },
         { scope: 'settlement', tabId: 'tab-seisan', baseLabel: '精算' }
     ];
     configs.forEach(({ scope, tabId, baseLabel }) => {
diff -ruN '--exclude=assets/vendor/carbon/carbon-entry.min.js' '--exclude=.git' carbon-refresh-base/assets/js/features/person-cards.js carbon-refresh-worktree/assets/js/features/person-cards.js
--- carbon-refresh-base/assets/js/features/person-cards.js	2026-08-20 02:39:08.000000000 +0000
+++ carbon-refresh-worktree/assets/js/features/person-cards.js	2026-08-20 02:53:03.001486563 +0000
@@ -9,9 +9,7 @@
     const n = parseInt(grade) || 0;
     if (n <= 0) return '';
     const gradeText = `${n}年`;
-    const tagValue = ['male', 'female'].includes(gender) ? gender : 'unknown';
-    const tagAttributes = window.SanpoTagTypes?.attributes('grade', tagValue, 'sm', gradeText) || 'type="gray" size="sm"';
-    return `<cds-tag class="grade-badge carbon-display-tag" data-grade="${n}" ${tagAttributes}>${gradeText}</cds-tag>`;
+    return `<cds-tag class="grade-badge carbon-display-tag" data-grade="${n}" type="gray" size="sm">${gradeText}</cds-tag>`;
 }
 
 function renderPersonFlag(flag) {
@@ -87,7 +85,7 @@
     const genderHtml = genderBadgeHtml(g);
     div.innerHTML = `
         <div class="member-main-line">
-            <span class="person-drag-affordance" aria-hidden="true"><span data-carbon-icon="draggable"></span></span>
+            <span class="person-drag-affordance" role="img" aria-label="ドラッグして移動"><span data-carbon-icon="draggable" aria-hidden="true"></span></span>
             <div class="member-name-text">${safeName}</div>
             <div class="person-meta">${renderPersonFlag(flag)}${genderHtml}${gradeHtml}</div>
             ${renderPersonOverflowMenu({ name, isDriver: false, inWaiting: parent?.id === 'waiting-list', locked })}
@@ -113,17 +111,18 @@
     const driverGradeHtml = renderGradeBadge(dgrade, dg);
     const driverGenderHtml = genderBadgeHtml(dg);
     const groupSuffix = typeof getActiveGroupSuffix === 'function' ? getActiveGroupSuffix() : '車';
+    const driverRoleLabel = document.body.dataset.activePlanTemplate === 'team' ? '班長' : '運転手';
     let slotsHtml = `
         <div class="driver-seat" data-gender="${dg}" data-name="${safeName}" data-participant-id="${escapeHtml(participantId || '')}" data-grade="${dgrade || 0}" data-flag="${normalizePersonFlag(dflag)}">
             <div class="member-main-line driver-main-line">
                 <div class="driver-name-disp ">${safeName}</div>
-                <div class="person-meta">${renderPersonFlag(dflag)}${driverGenderHtml}${driverGradeHtml}</div>
+                <div class="person-meta"><cds-tag class="driver-role-tag" type="gray" size="sm">${driverRoleLabel}</cds-tag>${renderPersonFlag(dflag)}${driverGenderHtml}${driverGradeHtml}</div>
                 ${renderPersonOverflowMenu({ name, isDriver: true })}
             </div>
             <div class="memo-popup driver-memo-text" style="display:${dm?'block':'none'}">${safeMemo}</div>
         </div>
     `;
-    for(let i=0; i<c; i++) slotsHtml += `<div class="seat-slot"><span class="seat-slot-icon" data-carbon-icon="add" aria-hidden="true"></span></div>`;
+    for(let i=0; i<c; i++) slotsHtml += `<div class="seat-slot"><cds-icon-button class="seat-add-btn" type="button" kind="ghost" size="lg" aria-label="メンバーを追加" align="top"><span data-carbon-icon="add" slot="icon" aria-hidden="true"></span></cds-icon-button></div>`;
 
     col.innerHTML = `
         <div class="car-box" data-capacity="${c}" data-group-id="${escapeHtml(groupId || '')}">
