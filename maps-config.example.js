// Copy this file to maps-config.js or generate it with `npm run build:maps-config`.
// Browser API keys are visible to clients by design. Restrict the key by HTTP referrer and API.
window.SANPO_GOOGLE_MAPS_CONFIG = Object.freeze({
  apiKey: "YOUR_RESTRICTED_BROWSER_API_KEY",
  language: "ja",
  region: "JP",
  version: "weekly"
});
